<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.lwb.adapter_demo">

    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BindingAdapter">
        <activity
            android:name=".ui.activity.DialogSelectActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activity.HeaderFooterActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activity.ExpandActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.MutableActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activity.MultiTypeActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activity.PagingActivity"
            android:exported="true" />
    </application>

</manifest>